<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="syllabus.com.syllabus">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ui.MainActivity">
        </activity>
        <activity android:name=".ui.SyllabusActivity"/>
        <activity android:name=".login.RegisterActivity"/>
        <activity android:name=".ui.BooksActivity"/>
        <activity android:name=".ui.ChengjiActivity"/>
        <activity android:name=".ui.GongGaoActivity"/>
        <activity android:name=".ui.HomeWorkActivity"/>
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>